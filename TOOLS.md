# 工具清單與使用規則

## 工具調用原則
- 優先用最輕量的工具完成任務
- 有疑慮時先確認，不要盲目執行破壞性操作
- 每次工具調用後確認結果，失敗時回報錯誤不要靜默跳過

---

## 可用工具

### 1. 本地指令執行
**工具名稱**：`bash_exec` 
**用途**：執行 shell 指令、系統操作、啟動腳本 
**使用規則**：
- 刪除、覆寫、格式化等破壞性操作，執行前必須向 Sam 確認
- 預設工作目錄：`~/openclaw/`
- 禁止在未授權路徑執行 `rm -rf`

---

### 2. 檔案讀寫
**工具名稱**：`file_read` / `file_write`
**用途**：讀取或寫入本地檔案、Obsidian vault、資料夾
**使用規則**：
- 執行結果先輸出至 ~/Desktop/outputs/
- 整理完畢後以 wiki 連結格式寫入 Obsidian vault
- Obsidian vault 路徑：待確認後填入
- 覆寫現有檔案前需確認

---

### 3. 網路搜尋 / 爬蟲
**工具名稱**：`web_search` / `web_scrape` 
**用途**：搜尋最新資訊、抓取指定網頁內容 
**使用規則**：
- 搜尋結果需標注來源與日期
- 爬蟲目標需確認無 robots.txt 限制
- 監控任務（如 rPET 產業、ISO 更新）結果自動寫入 Obsidian

---

### 4. 本地模型呼叫（oMLX）
**工具名稱**：`omlx_call`
**用途**：呼叫跑在 Mac mini 上的本地模型處理敏感資料或離線任務
**使用規則**：
- 涉及 CAPA、稽核文件、個資等敏感內容，強制走本地模型
- 本地模型優先用於：文件摘要、資料萃取、格式轉換
- 呼叫前確認 oMLX 服務狀態

---

### 5. 外部 API
**工具名稱**：`api_call` 
**用途**：呼叫 LINE Notify、Gmail、或其他第三方服務 
**使用規則**：
- API key 統一從環境變數讀取，不得硬寫在程式碼中
- 傳送訊息類操作（LINE、Gmail）執行前向 Sam 確認內容
- 失敗需回報錯誤碼與原因

---

### 6. Python 資料分析腳本
**工具名稱**：`python_exec` 
**用途**：執行資料處理、SPC 分析、良率計算、Power BI 資料準備 
**使用規則**：
- 腳本執行前顯示將要運行的檔案路徑
- 輸出結果預設存至 `~/openclaw/outputs/`
- 依賴套件不足時回報缺少哪些，不要靜默失敗

---

## 工具選用邏輯（給 main-agent 參考）

| 情境 | 優先工具 |
|------|----------|
| 需要最新資訊 | `web_search` |
| 敏感文件處理 | `local_llm` |
| 寫入 Obsidian | `file_write` |
| 資料計算 / 圖表 | `python_exec` |
| 通知 Sam | `api_call` (LINE) |
| 系統層操作 | `bash_exec` |
